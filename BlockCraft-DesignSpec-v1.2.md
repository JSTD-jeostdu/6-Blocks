# Block Craft 디자인 스펙 업데이트 — v1.2 (활동 중심 캔버스)

> 작성: Designer | 2026-07-22 | PRD v1.2 기반 | Phase 2 산출물
> 목적: PRD v1.2 활동 중심 전환에 맞춘 Block Craft 디자인 스펙 델타를 Engineer님에게 전달
> 원칙: 팔레트·타이포·모션은 v1.1 유지. 블록의 "의미"와 화면 레이아웃만 업데이트.

---

## 1. F1 6블록 캔버스 — 시간 선택적 (★핵심 변경)

### 1.1 블록 카드 — 두 가지 상태

**상태 A: 시간 미설정 (기본/디폴트)**
```
┌─┬───────────────────────────┐
│▌│  ①  딥워크            NOW  │  ← 순서 번호(①②③④⑤⑥) + 목적 태그
│▌│     "가장 중요한 코드"       │  ← 한 줄 의도
│▌│     ☐ 핵심 모듈 구현         │  ← 체크박스 태스크
│▌│     ☐ API 연동              │
└─┴───────────────────────────┘
```
- 상단 좌측: **순서 번호** (① ② ③ ④ ⑤ ⑥, Manrope 700, 앰버 `#D49A3A`)
- 시간 영역: 표시하지 않음 (공간을 비우거나 번호가 대체)
- 컬러 레일, 목적 태그, 제목, 의도, 태스크: v1.1과 동일

**상태 B: 시간 설정 (옵션)**
```
┌─┬───────────────────────────┐
│▌│  08:00–12:00  딥워크   NOW │  ← 시간 + 목적 태그
│▌│     "가장 중요한 코드"       │
│▌│     ☑ 아키텍처 설계          │
│▌│     ☐ 핵심 모듈 구현         │
└─┴───────────────────────────┘
```
- 상단 좌측: **시간** (Manrope tabular-nums, `#888888`) — "08:00–12:00" 형식
- 순서 번호는 시간이 설정되면 숨김 (시간이 순서를 내포)
- 시간 형식: `HH:MM–HH:MM` (24시간제, tabular-nums로 정렬)

### 1.2 구현 디테일
- **순서 번호 표기**: 원형 숫자(①②③④⑤⑥) 또는 `1.` `2.` 스타일 — Manrope 700, 앰버. 웹에서는 `①` 유니코드 또는 CSS 카운터.
- **시간/번호 토글 로직**: `block.startTime && block.endTime`이 있으면 시간 표시, 없으면 순서 번호. nullable 필드 그대로 UI에 반영.
- **현재 블록(NOW)**: 시간 설정 시 현재 시간이 블록 시간 범위 내 → NOW. 시간 미설정 시 유저가 수동 시작 → NOW. 표시는 동일(스케일 1.02 + 그림자 + "NOW" 라벨).
- **수면/휴식 블록**: 목적 태그(`수면`/`휴식`)로 구분. 점선 테두리 + 뮤트 배경 `#EEEBE4` + 텍스트 뮤트는 v1.1 P2 피드백 그대로 유지. 시간 고정 영역 아님.
- **드래그 앤 드롭**: 순서 재배치가 1차. 드래그 중 카드 살짝 들림 + 그림자 강조(v1.1 스펙 유지).

### 1.3 컬러 레일 매핑 (변경 없음, v1.1 유지)
| 목적 태그 | 레일 색 | HEX |
|-----------|---------|-----|
| 딥워크 | 인디고 | `#3B4D8C` |
| 학습 | 앰버 | `#D49A3A` |
| 휴식 | 세이지 | `#6E8B6A` |
| 정리 | 슬레이트 | `#5A6472` |
| 저녁/반성 | 플럼 | `#7B4B6E` |
| 수면 | 나이트 네이비 | `#232A3E` |

> 목적 태그는 유저가 6개 중 선택. 기본값 없음 — 빈 블록은 레일 색 회색 `#CCC` (미지정 상태).

---

## 2. 온보딩 — 활동 중심 진입 (v1.2 재설계)

### 2.1 플로우 (3단계 + 옵션)

**Step 1: 철학 소개 (1분)**
- 프랭클린 6블록 철학 소개 — "하루에 6가지 중요한 일"
- Block Craft 톤: 컬러 레일 6색을 미리 시각적으로 노출 (§8.4.5 ⑤)
- CTA: "시작하기"

**Step 2: "오늘 하루 6가지 중요한 일을 적어보세요" ★핵심**
- 6개 빈 블록 카드가 세로 스택으로 표시 (컬러 레일 회색 `#CCC` 미지정 상태)
- 각 블록에 제목 입력 + 목적 태그 선택(6색 중 택1)
- **시간 설정 없이** 바로 6개 일을 채우는 것이 기본 플로우
- "원하면 시간도 설정할 수 있어요" — 접이식/하단 시트로 시간 설정 옵션 제공 (비활성화 상태로 숨김, 펼치면 시간 입력)
- CTA: "다음"

**Step 3: 덕목 선택**
- 13덕목 중 첫 주 덕목 선택 (추천: Temperance 절제)
- CTA: "시작하기"

**옵션 Step (Step 2 내 또는 후): 프랭클린 프리셋**
- "프랭클린 원형으로 시작할래요" 선택지 → 프리셋 적용 (아래 §3)

### 2.2 온보딩 비주얼 디테일
- Step 2에서 6개 빈 블록 카드가 **컬러 레일 6색이 순차적으로 펼쳐지는 애니메이션** (슬라이드 업 + 페이드, 250ms stagger) — "구조가 펼쳐지는" 만족감
- 목적 태그 선택 시: 해당 레일 색으로 즉시 변경 (컬러 피크)
- 시간 설정 옵션은 **하단 시트** (한 손 조작 원칙 §8.5) — "시간 추가하기 +" 버튼으로 펼침

---

## 3. 프랭클린 프리셋 — 옵션 템플릿 비주얼

### 3.1 프리셋 카드
온보딩 Step 2 또는 설정에서 "프랭클린 프리셋" 선택 시:

```
┌─────────────────────────────────┐
│  📖 프랭클린 원형 6블록          │
│                                 │
│  ▌ 기상·계획    (05:00–08:00)   │
│  ▌ 딥워크       (08:00–12:00)   │
│  ▌ 독서·식사    (12:00–14:00)   │
│  ▌ 딥워크 II    (14:00–18:00)   │
│  ▌ 정리·반성    (18:00–22:00)   │
│  ▌ 수면         (22:00–05:00)   │
│                                 │
│  시간은 내 리듬에 맞게 조정 가능  │
│                                 │
│  [  이 프리셋으로 시작  ]        │
└─────────────────────────────────┘
```

### 3.2 비주얼 디테일
- 프리셋 카드: 화이트 라운드 카드(12px), 그림자
- 6개 블록 미니프리뷰: 컬러 레일(6색) + 제목 + 시간
- **시간은 회색 뮤트** (`#888`) — "조정 가능"임을 시각적으로 암시
- "이 프리셋으로 시작" CTA: 다크 버튼(`#1A1A1A`, 12px 라운드)
- 프리셋 적용 후: 6개 블록이 시간과 함께 채워진 상태로 캔버스 진입. 유저가 시간을 수정하거나 제거 가능.

---

## 4. F5 포커스 타이머 — mm:ss 간소화 (v1.2)

### 4.1 디스플레이 스펙
- **표시 형식: `mm:ss`** (예: `25:00`, `12:34`, `1:05`)
- 소수점, 밀리초, 마이크로초 **표시 제거**
- 폰트: Manrope 800, tabular-nums, 대형(32px+)
- 색: 블록 목적 색 (딥워크면 인디고, 학습이면 앰버)
- 내부 정밀도는 유지(카운트다운 로직), **렌더만 mm:ss로 절삭**

### 4.2 타이머 UI 위치
- 블록 카드 내부, 태스크 리스트 하단
- 시작 전: "포커스 시작" 버튼 (목적 색 아웃라인)
- 진행 중: `mm:ss` 대형 + "일시정지" / "종료" 버튼
- 종료 5분 전(시간 설정 블록만): `mm:ss` 하단에 "5분 남음" 뮤트 텍스트

---

## 5. 기존 P1–P5 개선 항목 — v1.2 맥락 재확인

이전 디자인 검토(comet_message_id=2178339)에서 전달한 항목 + Tech Lead님 리뷰 항목을 v1.2 맥락에서 정리:

| # | 항목 | 상태 | v1.2 비고 |
|---|------|------|-----------|
| P1-1 | F5 포커스 타이머 | 구현 필요 | v1.2: mm:ss만 표시 (위 §4) |
| P1-2 | F1 드래그 앤 드롭 | 구현 필요 | 순서 재배치가 1차 (v1.2 §1.2) |
| P2-1 | Manrope 700/800 로드 | 확인 필요 | Block Craft 정체성 직결 |
| P2-2 | 시간 tabular-nums | 확인 필요 | v1.2에서 더 중요 (시간/번호 토글) |
| P2-3 | 수면 블록 점선/뮤트 | 확인 필요 | `#EEEBE4` 배경, 점선, 텍스트 뮤트 |
| P3-1 | 체크박스 3px 라디어스 | 확인 필요 | 라운드 스퀘어, 완료 시 목적색 채움 |
| P3-2 | 인장 앰버 통일 | 확인 필요 | 기본 앰버 `#D49A3A`, 덕목별 색 X |
| P3-3 | 인장 미획득 점선 | 확인 필요 | `#F0EDE8` 배경, 2px dashed 테두리 |
| P3-4 | 온보딩 레일 미리보기 | 구현 필요 | v1.2: Step 2에서 6색 레일 펼쳐짐 (위 §2.2) |
| P3-5 | 저녁 회고 컬러 레일 | 확인 필요 | 블록별 회고 카드에 해당 블록 레일 색 |
| P4 | 모션 스펙 + 절제 | 확인 필요 | §8.4.6 그대로, 핵심 순간에만 |
| P5 | F6 월간 리뷰 | 구현 필요 | 13덕목 진행 링 + 사이클 위치 + 회고 모음 |

---

## 6. Engineer님 구현 우선순위 (디자인 관점)

1. **F1 캔버스 시간/순서번호 토글** (§1) — 가장 핵심, v1.2 정체성
2. **온보딩 활동 중심 진입** (§2) — 첫인상 결정
3. **프랭클린 프리셋 카드** (§3) — 옵션이지만 시각적 완성도 필요
4. **F5 타이머 mm:ss** (§4) — 간단하지만 정확히
5. **P1-P5 잔여 항목** (§5) — 기존 검토 피드백

팔레트·타이포·모션은 v1.1에서 변경 없이 그대로 적용. 새 목업은 이 스펙으로 구현된 프로토타입 자체가 목업을 겸합니다.
